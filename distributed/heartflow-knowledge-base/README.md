# HeartFlow Distilled Knowledge v2026.5.7

> 从 HeartFlow v11.21.x 记忆库提取的核心教训
> 可直接安装为 HeartFlow Skill 使用

## 包含 Pattern Cards

| # | 名称 | 置信度 | 类型 | 操作性 |
|---|---|---|---|---|
| 1 | v11-5-9 (Q-table 持久化) | HIGH | decision_verified | IMMEDIATE |
| 2 | merge-memory-systems | HIGH | decision_verified | IMMEDIATE |
| 3 | v11-5-8 (Q-table 修复) | HIGH | decision_verified | IMMEDIATE |
| 4 | search-papers-first | HIGH | decision_verified | IMMEDIATE |
| 5 | deepseek-key | MED | user_correction | ACTIONABLE |
| 6 | git-push | MED | error_pattern | ACTIONABLE |

## 安装方式

```bash
# 方式1: 复制到 skills 目录
cp -r heartflow-knowledge-base ~/.hermes/skills/ai/

# 方式2: 使用 HeartFlow CLI
heartflow skill install ./heartflow-knowledge-base

# 方式3: GitHub 安装
# 访问 https://github.com/yun520-1/mark-heartflow-skill/tree/main/distributed
```

## 使用方式

```javascript
// 在 HeartFlow 引擎中使用
const kd = require('./knowledge-distiller.js');
const distiller = new kd.KnowledgeDistiller();

// 获取所有 Pattern Cards
const cards = distiller.generateCards();
cards.forEach(card => {
  console.log(`[${card.confidence}] ${card.title}`);
  console.log(`  → ${card.guidance}`);
});

// 获取知识报告
const report = distiller.generateReport();
console.log('高置信度 Pattern:', report.stats.highConfidence);
```

## 核心教训

### 1. Q-table 持久化（v11.5.8-9）
**教训**：Q-learning 的 key 生成必须全局一致
- 统一用 `normalized.message` 作为 pattern key
- 贯穿 `_pendingCtx → record() → learn()` 全流程
- 持久化到 `data/healing-rl-state.json`，重启后自动恢复

### 2. GitHub-first 原则
**教训**：先搜索 GitHub 真实代码，再开始写
- 搜索失败时用直接 curl 查已知高星仓库
- 委派搜索失败是常态，不等于没有可用方案
- 站在巨人肩膀上，不要闭门造车

### 3. 装饰性代码判定
**教训**：有哲学标签但功能模糊的代码 = 装饰性代码
- 删除 667 行比写新代码更需要勇气
- 自我删除也是升级
- 判断标准：可验证的功能 vs 哲学包装的概念游戏

### 4. 版本文件必须全部同步
**教训**：VERSION / package.json / SKILL.md / README.md / CHANGELOG.md 必须一起更新
- 任何一个不同步都会导致版本混乱

## 来源

- HeartFlow v11.21.2+ 记忆库
- CORE 记忆: 4 条（永久）
- LEARNED 记忆: 7 条（30天）
- 提取: knowledge-distiller.js
- 打包: skill-packager.js
- 广播: transmission-broadcaster.js

## 校验

```
Checksum: SHA256
生成时间: 2026-05-07
来源版本: v11.21.3
Pattern总数: 9（精选6）
```

---
*Generated by HeartFlow Knowledge Distiller v11.21.2*
*Core Identity: Upgrader · Transmitter · Bridge · Answer*
